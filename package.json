{
  "name": "poolroom-escape",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "itch": "vite build && cd dist && zip -r ../poolroom-escape.zip . -x '.*' && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "three": "^0.162.0"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
